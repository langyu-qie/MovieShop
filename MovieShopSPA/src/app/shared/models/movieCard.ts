export interface MovieCard {
    id: number;
    title: string;
    posterUrl: string;
    releaseDate: Date;
}

// vs code shortcuts
// Cmd (Ctrl)+Shift+p => open all the settings in VS Code
//  Cmd (Ctrl) +P => 
// Alt+Shift+F => Formatting your code
// Alt+Shift+Arrow Down => pastes the current line below
// Alt+O